# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

## git-lfs ##

OpenKCC/Build/*.data filter=lfs diff=lfs merge=lfs
OpenKCC/Build/*.wasm filter=lfs diff=lfs merge=lfs